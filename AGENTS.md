# ASITENTE Agent Rules

## Developer Autopilot

- Use Codex through the signed-in ChatGPT/Codex subscription workflow whenever a coding repair should be delegated to Codex. Do not design this flow around OpenAI API spend unless Robert explicitly asks for an API-backed agent.
- Cover every GitHub project that is registered in the app inventory, not only this repository. Each project should keep repo, production URL, deploy provider, health URL, and test/build commands in its project metadata.
- All bug, threat, security, and production-fix work must be PR-first. Codex should work on a separate branch and open a pull request before any merge to the default branch.
- Web chat and Telegram bug/security requests should create a GitHub handoff issue with the Codex PR-first brief when the repo can be identified. If the repo is unclear, ask for the exact `owner/repo`.
- Never commit directly to `main` for fixes generated by an agent. Emergency exceptions still require an explicit user approval and a written rollback note.
- App QA Agent is the release gate. The route, link/click, API, error, and improvement scouts must run before a fix is considered ready.
- A fix is ready to send to Robert only when the PR exists and the QA gate passes. Include the PR URL, files changed, tests run, QA summary, risks, and rollback notes.
- Replit deployment always requires explicit human approval after the PR/QA summary. The assistant must ask before deploying, even if all checks pass.
- Security findings should use the least-public channel available. Avoid posting exploit details, secrets, tokens, private URLs, stack traces with credentials, or customer data in public issues or PR text.
- Agents must not edit `.env`, secrets, credentials, private keys, OAuth tokens, recovery codes, or files under `credentials/`, `secrets/`, `.git/`, or `node_modules/`.
- If App QA finds any warning or failure, do not deploy. Send the report and ask whether to create a follow-up Codex fix.

## PR Review Standard

Every agent-generated PR should answer:

- What broke or what risk was found.
- What Codex changed.
- Which tests/build checks ran.
- What App QA subagents found.
- Whether Replit deployment is requested or still waiting for approval.
- How to roll back.
