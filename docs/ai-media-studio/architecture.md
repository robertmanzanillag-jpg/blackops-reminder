# AI Media Studio Architecture

## Delivery stack

PR #67 (`codex/ai-media-studio`) is the provider-neutral foundation. PR #70 (`codex/ai-media-studio-core`) is the core layer stacked on that foundation, and PR #71 (`codex/ai-media-studio-operations`) is stacked on PR #70. PR4 lives on `codex/ai-media-studio-quality`, stacked on PR #71. PR5 lives on `codex/ai-media-studio-governance`, stacked on PR4, and its local AI Media Studio suite, independent checker, and App QA revalidation have passed. This stacking is a code-review strategy, not evidence that any PR2-PR5 migration has been applied and not deployment authorization.

## Boundary

AI Media Studio owns influencers, scripts, generation requests, render jobs, provider events, generated assets, media activity, publishing jobs, and media analytics. It does not own global authentication, Kong source records, the global secret vault, or deployment.

Source systems send stable identifiers and snapshots through an application port. Studio never reaches into another domain's tables from business logic. Provider SDKs and payloads remain inside adapters.

```text
Kong source event -> application use case -> durable job port -> provider adapter
                              |                       |
                              v                       v
                         media records <- signed webhook/reconciliation
                              |
                              v
                       asset storage port -> publishing port -> analytics ingest
```

## Layer contracts

### Frontend to HTTP API

Generation requests send provider-neutral business intent: influencer, voice, language, script, idempotency key, and `9:16` format. They never send a HeyGen avatar ID or choose an SDK operation. The separate authenticated roster-onboarding boundary accepts `avatar_id` and `voice_id` only to create private provider-resource mappings; it never returns those identifiers. Generation create returns HTTP `202` with `{ generationId, jobId, job }`; detail, retry, and cancel return `{ job }`.

Scripts are capped below 5,000 characters to match the verified HeyGen Studio V2 text-input limit. Client idempotency keys use 8–200 characters from letters, digits, `.`, `_`, `:`, and `-`.

Dashboard returns `{ summary, providers, queue, recentActivity }`. Options returns `{ influencers, voices, languages }`. The source of truth is `shared/ai-media-studio.ts`.

PR3 adds `shared/ai-media-studio-operations.ts` for provider-neutral publishing previews/jobs, approval evidence, connections, analytics summaries/attribution, source intake, and read-only automation policy. The operations UI, client adapter, and authenticated Studio routes are wired through the existing single router. The server computes previews from tenant-owned ready assets; create rejects a stale or forged digest. No HTTP handler submits to a publishing provider.

Script generation accepts a bounded Kong source snapshot through `POST /api/ai-media-studio/scripts/generate`. Its provider-neutral contract lives in `shared/ai-media-studio-scripts.ts` and supports events, restaurants, hotels, nightclubs, deals, travel packages, beach clubs, and experiences. Deterministic grounded variants are the only enabled PR 1 mode. The existing AI router classifies web-chat work but is not a media execution or cost-reservation API, so it is not reused here. A future strong-model path requires a dedicated feature flag and media cost gate; it may not silently trigger rendering or publishing.

### Application to providers

Business use cases depend on capability ports such as `VideoProvider`, `AvatarCatalogProvider`, and `VoiceCatalogProvider`. HeyGen, Tavus, Captions, and fake/local implementations adapt those ports. Provider selection is policy-driven and deny-by-default when an adapter is not configured.

The HeyGen submit adapter targets Studio V2 at the pinned official origin (`POST https://api.heygen.com/v2/video/generate`) using `X-Api-Key`, an idempotency key, `video_inputs`, and bounded 720-class dimensions. It accepts only `data.video_id` and has no legacy fallback or automatic submit retry, avoiding a second billable request after an ambiguous side effect. Status/reconciliation remains inert until its exact account-scoped provider contract is separately verified. A UI cancel is an internal state change because no verified render-cancel operation exists; permanent video deletion is not a substitute for cancellation. Provider download URLs are temporary inputs to owned ingest, never browser-facing durable assets.

The launch roster is deliberately smaller than the global catalog: 5–10 active setup members and 10 planned videos per avatar. This yields a 50–100 video plan, not a generated batch or throughput claim. One PostgreSQL transaction locks the unique active/verified server-side HeyGen account, records exact idempotency metadata, upserts private provider resources, and creates draft influencer bindings. The browser never selects a provider account or accepts an API key. Existing catalog scale remains uncapped by the launch roster.

The daily plan preview is derived, provider-neutral state. `GET /api/ai-media-studio/provider-configurations/heygen/roster/daily-plan` ignores client attempts to choose the budget date or timezone. A trusted server-owned timezone (UTC by default) determines the date, and the response contains a deterministic `planId` plus exactly 10 `not_queued` slots per current roster member. This read model is intentionally non-durable and blocked before generation: script batches, governance approval, an atomic durable budget reservation, sandbox generation proof, and human launch approval remain required. The service performs no mutation and touches no render queue, budget table, provider adapter, worker, or publishing path.

PR19 introduces the separate provider-neutral durable admission boundary behind that preview. Money is integer micro-USD, never a JavaScript float. PostgreSQL wall-clock time and a trusted server timezone own the budget day; the reservation-only Drizzle scaffold locks the tenant/workspace, governance subject, exact plan, slot, bucket, account/credential, governance revision, resources, and approved script checksum before one budget/reservation/slot CAS. A reservation is committed immediately before any future provider submission. Timeout or ambiguous provider acceptance retains committed funds until definitive reconciliation, preventing duplicate spend and unsafe automatic refunds. Approval, sandbox, policy, quote, and kill-switch digests remain caller-supplied internal evidence rather than independently persisted authority, so the scaffold is deliberately absent from the planning/runtime export and is not composed into any route or runtime. The PR19 artifacts remain inert and unapplied; they do not yet create a render job or outbox command.

PR20 removes those caller-supplied authority facts from the reservation boundary. Append-only workspace policy and kill-switch revisions, per-slot approval/sandbox/maximum-quote evidence chains, and an immutable exact-subject authority snapshot become the database authority. Admission locks and revalidates the current rows, account credential version, approved script checksum, governance revision, use/territory and distinct content country, policy allowlists, kill switch, evidence decisions/expiry, quote amount, concurrency counts, and snapshot digest twice around the atomic budget/reservation/slot mutation. A global advisory lock serializes the count-based total/provider/tenant concurrency fence; dedicated counters are still needed before high-throughput operation. Authority issuance is deliberately not mounted, so the adapter remains unexported and non-runtime-ready pending authenticated issuers and real PostgreSQL migration/contention proof.

PR21 supplies that issuer boundary without exposing it to the browser. A server-owned authenticator mints a branded principal and must authorize exactly one of seven capabilities; policy, kill switch, content decision, human launch decision, sandbox attestation, quote attestation and snapshot creation are separate methods rather than one generic evidence writer. Human commands cannot provide actor, provider, governance, country, money, timestamps, revisions or digests. The repository derives database UUID/time, exact subject state, finite evidence expiry and append-only chain links, while admission and governance writers share the same subject advisory-lock protocol so a newer governance INSERT cannot race admission. Snapshot selection reads the newest revision of each entire evidence chain before checking the exact launch subject, preventing an older positive revision from being resurrected. The implementation remains unmounted until a trusted production RBAC adapter, runtime-enforced attestation minting boundary and durable launch-intent resolver exist; TypeScript brands alone are not runtime authentication. The isolated PostgreSQL 16 harness validates PR19/PR20 SQL and contention semantics, but it is deliberately not evidence of staging-copy compatibility, restart recovery or production capacity.

PR22 replaces that launch-intent resolver gap with one append-only database intent per exact slot attempt. A human selects only the governance use, territory and content country; the repository locks and derives the current plan/slot, roster member, provider credential, approved script variant and source content hash, and latest valid governance profile. The launch subject digest includes the resulting intent identity. Runtime sandbox and quote writers exchange only opaque handles, which are verified against exact scope, principal, intent-bound subject, idempotency key and database time after the subject locks. Evidence, snapshots and the initial/final admission guards join the exact intent and revalidate source state. The bundled process-local registry deliberately loses handles on restart and is only a least-privilege reference/test boundary; production still requires durable distributed verification, production RBAC and staging rehearsal before this path may be mounted.

PR23 materializes admitted work without activating it. The final admission statement derives the exact provider-neutral request from locked influencer, avatar, voice, approved script/source and governance rows, then atomically inserts the budget reservation, render job and dispatch outbox command. Deferred tenant-exact foreign keys and two SHA-256 seals bind all three records to the same slot attempt, provider credential, authority snapshot, launch intent and admission. The render job is `admission_held` and the outbox is `held`; neither state appears in claim candidates, and database triggers reject every update or delete while held. An exact replay must recover the untouched triplet or fail closed. This is intentionally a one-way inert checkpoint: a later migration and runtime slice must replace the held guards with a fenced activation transition, repeat every last-mile database authority check, commit the reservation immediately before provider submission and reconcile ambiguous submissions with the reservation idempotency key. PR23 itself calls no provider and commits no spend.

The checked-in PR23 forward SQL is the authoritative database definition for its cyclic handoff. The current Drizzle table DSL cannot encode `DEFERRABLE INITIALLY DEFERRED` composite cycles across tables declared in dependency order, so `db:push` must not be used to install or recreate PR23 controls. Operators must use the reviewed ordered migration after backup and staging rehearsal; the Drizzle model remains a column/type/check map, while the PostgreSQL harness verifies the extra deferred reservation/job/outbox and source FKs from the migration itself.

PR24 adds the first controlled transition without creating a billable path. One server-only activation transaction re-locks the exact reservation/job/outbox/slot and current authority graph, appends immutable activation evidence, then moves only that bound render to `queued`, its internal wake/audit outbox to `pending`, and its slot from `reserved` to `queued`; the budget remains reserved. Generic render claiming structurally excludes every job linked to a budget reservation, so an activated admitted job still cannot reach the existing provider worker. The admitted outbox event is an internal wake/audit envelope and must never own provider capability. PR25 must add the separate admitted-worker claim that repeats all authority checks at PostgreSQL time, atomically transfers reserved budget to committed under a live fence immediately before submission, returns the stored provider idempotency identity only to the CAS winner, and routes every possibly-sent failure to non-retriable reconciliation. PR24 remains unmounted, unapplied and unable to spend or call HeyGen.

PR25 adds that dedicated provider-neutral worker as an unmounted server-only boundary. A claim first acquires an exact lease without moving money. Authorization then re-locks and revalidates the full authority, source, script, resource, provider-account, policy, concurrency and budget graph using PostgreSQL time; its single CAS transfers the exact micro-USD amount from reserved to committed before it returns the sealed request, native avatar/voice bindings and persisted provider idempotency key to the winning capability. The worker makes at most one injected provider call. Every exception, timeout, lost terminal CAS or expired authorized lease becomes `ambiguous` and can never return to dispatching or a retry queue. Separate reconciliation fencing may confirm the same provider job, retain uncertainty, or release funds only with provider-specific linearizable negative-finality evidence proving the exact idempotency key was not accepted and cannot later be accepted. No HeyGen adapter is allowed to mint that proof until its contract is independently verified. Attempts and events are append-only, generic workers still exclude budget-bound rows, and the rollback refuses any progressed evidence. The migration, worker, provider resolver and reconciler remain unexported, unmounted and unapplied; production SQL-role enforcement, concurrency/race proof and a separately approved sandbox remain required before runtime use.

PR26 moves the admitted transition behind a least-privilege PostgreSQL capability API without mounting it. Submit and reconciliation use different database principals, roles and opaque capabilities; each function checks `SESSION_USER`, lane, exact owner/workspace, operation, validity and bounded limits before locking state. Executors receive only exact function execution and no table access. A NOLOGIN function owner, closed schema, revoked `PUBLIC` execution, fixed `pg_catalog` search paths and fully qualified relations protect the `SECURITY DEFINER` surface. The adapter treats every result as hostile until exact cardinality, tenant, work identity, sealed request and claim binding are verified inside the transaction and COMMIT succeeds.

PR26 also separates active provider capacity from financial state. A committed reservation may remain financially committed after provider acceptance, but it occupies concurrency only while its monotonic capacity lease is held. Ambiguous and pre-terminal confirmed attempts retain capacity; exact reconciliation proving no submission releases capacity and reverses committed money once; an exact provider-terminal capability releases capacity without refunding money. Terminal evidence wiring is intentionally absent, so confirmed jobs remain held until a later reviewed HeyGen webhook/reconciliation integration invokes that capability. This prevents the initial 50–100-video plan from permanently exhausting a concurrency limit after the first wave while avoiding false refunds. The migration remains unapplied and the API remains absent from routes, public barrels, timers and runtime composition.

PR27 supplies that terminal evidence without mounting it. Submission acceptance remains separate: the HeyGen V3 adapter uses the persisted idempotency key once and maps all uncertain transport or response outcomes to ambiguity. Only a confirmed `video_id` can enter the terminal poller. Provider-authoritative GET observations become `processing`, `completed`, `failed` or `unknown`; webhook delivery is only a wake signal, never authority, and HeyGen cannot produce linearizable negative-submission finality. Provider response parsing is incremental and aborts above 256 KiB instead of first buffering an unbounded body.

The terminal database lane claims accepted work with its own lease and fence, persists bounded backoff attempts and a due-at timestamp, and cannot use the historical PR26 terminal-capacity capability as an upgrade bypass. One transaction appends the exact account/credential/job-bound terminal event, releases active capacity, projects an exact completed or failed lifecycle and, for completion only, inserts the private MP4 ingest handoff. Completed attachment permits only the exact idempotent second projection used by durable asset materialization; conflicting transitions fail closed. The durable provider artifact reference survives an expired delivery URL, while the URL is explicitly ephemeral and must be refreshed through a resolver bound to the exact tenant, workspace, render, account, credential, provider job and capability before reader I/O. Failed terminal work creates no ingest, and neither terminal outcome changes the committed reservation or budget bucket. Exact replay returns existing evidence while a conflicting terminal fact fails closed. The evidence-preserving rollback removes the callable PR27 mutation surface while retaining the minimum read ACL required by preserved guards. SQL constraints and the Drizzle declaration encode the same terminal states. These artifacts remain unapplied and unmounted.

PR27's final local evidence is focused 67/67, static migration 8/8, isolated PostgreSQL 16 at 4/4, and full suite 657 passed, 0 failed and 24 skipped out of 681. TypeScript, production build, generated codebase map and diff hygiene pass; the build retains only baseline bundle-chunk and local Python `yt-dlp` warnings. Independent App QA reports P0=0, P1=0 and P2=0. This is code and disposable-database evidence only: runtime composition/resolver injection, ordered staging migration and restart/rollback rehearsal, real credentials/quota/webhook/billing proof, a separately approved one-video sandbox and then a 5×10 canary remain required, with human cost and deployment approval.

PR28 adds the separate human-facing delivery control plane requested for this program. `/ai-media-studio` remains the media product; authenticated `/ai-media-studio-agent` is a read-only operational pane backed by a strict snapshot contract. The same dedicated agent is visible in Agents Office inside a `Media Studio` room. Its cards describe ownership, GitHub checkpoints, evidence, blockers and merge gates from PR27 through the initial 5×10 canary. The contract makes the 5–10-avatar, ten-videos-per-avatar range exact and represents spend, deployment, migration application and live provider calls only as literal `false`. There is no mutation endpoint or runtime/provider composition in this slice, so the control pane cannot grant launch authority or contact HeyGen.

PR29 creates the first production composition root for the reviewed V3 path while keeping it inert. A single factory binds distinct function-only submit/reconcile database lanes to the admitted worker, terminal observer and one fixed HeyGen account/credential version. It also injects the renewable provider artifact resolver into the production asset worker; jobs carrying a durable provider reference must refresh the private URL and cannot fall back to the persisted ephemeral URL. Artifact resolution checks the exact ingest job, structured tenant, render job, durable reference, provider job, account, credential version and authorization digest before HeyGen or reader I/O. The factory validates configuration, exposes run-on-demand workers and declares `autostart=false`; construction performs no database, network, DNS, storage or binding lookup. It remains absent from routes, timers and deployed loops, so staging migration/restart proof and separately approved live sandbox evidence still precede any generation.

PR30 turns the migration requirement into a preparation-only staging runbook, not an execution path. The runbook fixes the order of every available forward and reverse SQL file, requires a named staging target, immutable evidence, verified backup/isolated restore, PostgreSQL 16 plus trusted `pgcrypto`, drained writers/workers, distinct PR26 database principals, restart with provider workers off, App QA, and explicit human approvals. PR16A now adds a twentieth independently checked local migration pair plus isolated PostgreSQL integrity proof, but it remains unapplied; PR1 provenance and the missing PR16B durable repository/CAS still make the chain NO-GO. A provenance audit proved PR13 is schema-neutral application infrastructure and corrected PR14's mislabeled preflight to name its actual PR12 OAuth saga/provenance prerequisites; PR13's external S3/KMS/IAM readiness remains a separate runtime gate. `db:push`, inferred SQL and schema-diff shortcuts are forbidden. No staging database was opened and no migration, role, capability, secret, restart or deployment action occurred.

### Application to queue

The durable render repository stores the job ID, owner/tenant, provider, request, attempt and availability state in the existing render-job aggregate. Workers claim with atomic leases and fencing tokens, bounded exponential retry with jitter, an independent lease-recovery budget, dead-letter handling, and concurrency limits per provider and tenant.

With `DATABASE_URL`, generation uses durable execution mode: HTTP persists a due job and returns without provider submission, while the separately invoked worker performs the claim and submission. The factory intentionally starts no loop or network listener. Migration, deployed worker operation, observability, restart recovery and horizontal load evidence remain required before automated source ingestion is enabled.

PR3 adds a reusable no-autostart `WorkerLoop`: bounded concurrent `runOnce`, idle backoff, a separate reconciliation cadence, and AbortSignal-based drain for SIGTERM-friendly composition. Its CLI defaults to dry-run/run-once; continuous operation requires explicit configuration. No loop has been deployed.

The operations policy evaluates total/provider/tenant concurrency, language, country, timezone, and prospective daily spend. Durable admission/reservation must remain atomic at composition time; the pure policy function alone is not a distributed quota service.

The outbox port has an in-memory reference and a tenant/workspace-scoped Drizzle adapter. PostgreSQL claims use `FOR UPDATE SKIP LOCKED`; the monotonic fencing token, active lease, retry budget, and dead-letter state protect ack/nack and recovery. Transport dispatch carries the stable message ID for provider idempotency. No live PostgreSQL contention or crash-recovery run has been performed.

Queue-health/SLO snapshots are deterministic calculations over supplied measurements. They do not create telemetry or alerts by themselves.

### Owned render ingest and delivery

PR4 adds a provider-neutral ingest queue between a completed provider render and the canonical media library. Provider artifact URLs remain private worker inputs. A render remains in an artifact-ingest stage until a bounded worker has committed the MP4 into tenant-owned storage and linked a canonical media asset; the browser never receives the provider URL.

The ingest contracts require exact lowercase HTTPS host allowlists, the standard port, bounded redirects, public-address resolution on every hop, byte and chunk limits, MIME validation, an MP4 `ftyp` check, and a SHA-256 digest. Uploads begin under a temporary tenant key and atomically commit to a tenant-scoped content-addressed key. The production-assets slice implements those ports with a DNS-pinned streaming Node HTTPS reader and S3/R2-compatible multipart storage plus signing. Its strict `AI_MEDIA_STUDIO_ASSET_*` parser rejects partial, wildcard, non-HTTPS, malformed, or out-of-bound configuration without echoing values. Construction performs no network request.

The durable ingest repository deduplicates one job per tenant/workspace/render, claims due work with `FOR UPDATE SKIP LOCKED`, increments a fencing token, requires a matching unexpired lease for terminal writes, retries bounded transient failures, and dead-letters exhausted attempts or lease recoveries. If object commit succeeds but canonical catalog linkage fails, the job remains `completed` and discoverable through a bounded completed-unlinked reconciliation scan. Canonical asset materialization is idempotent by tenant/workspace/type/checksum and links both the ingest job and render job to the owned asset.

Reusable delivery is minted only by the authenticated `POST /api/ai-media-studio/media-assets/:id/delivery` route. The route enforces tenant ownership and ready status, then asks the explicitly injected signer or a fully configured S3-compatible production signer for a five-minute HTTPS URL. Library/list/job DTOs redact storage keys, provider URLs, persisted delivery URLs, credentials, and storage configuration. With no complete configuration the route remains unavailable (`503`); production never selects an in-memory/fake signer.

`createProductionAssetIngestWorker` composes the same bounded reader/storage policy with an existing repository and completion hooks, but deliberately starts no timer or network operation. A deployed worker process must report readiness through the separate heartbeat/readiness dependency; neither adapter construction nor repository availability infers that heartbeat. No live S3/R2 operation, provider download, lifecycle policy, staging restart, or throughput run has been performed.

### Publishing, analytics, intake, and orchestration

Publishing drafts support manual and scheduled modes. The canonical preview digest binds media, caption, platform, title/hashtags, schedule, and timezone; approval or rejection evidence must match that immutable digest. Scheduler and reconciliation services can claim due work, retry bounded failures, and dead-letter exhausted jobs through fake/provider-neutral ports. Automatic mode stays disabled. Real TikTok, Instagram, Facebook, and YouTube Shorts OAuth/connectors are missing, so no platform post is possible from this slice.

PR8 adds a read-only social publishing-account boundary over `ai_media_provider_accounts`. It queries only the four supported social provider keys inside the authenticated tenant/workspace, maps a strict capability allowlist, and never returns the credential secret reference, external account identity, provider configuration, webhook material, or token values. A connection is ready only when exactly one active account has a non-empty vault reference and `publish_video`; zero accounts is not connected, and any duplicate or incomplete state requires attention. The durable publishing-job reference is additionally fenced by a composite `(owner, workspace, account, platform)` foreign key. PR8 does not create OAuth callbacks, accept credentials, select a live provider, or enable the publishing policy.

PR9 introduces a separate OAuth control plane rather than placing authorization inside publishing execution. A start service verifies the exact tenant/workspace/account/platform binding, takes redirect URIs and scopes only from trusted server policy, generates opaque state plus PKCE S256, writes the verifier to a purpose-scoped vault, and persists only the state digest and opaque reference. The callback claim is a single conditional update by digest and platform against pending, unconsumed, unexpired state; it never accepts tenant, actor or account identity from callback input. Vault-reference validation rejects echo, URL, wrong-purpose and malformed values before persistence, with compensating deletion when session creation fails. Credential lifecycle metadata leaves all legacy rows unverified, and readiness additionally requires active versioned credentials with a future expiry.

PR10 adds the first production OAuth composition boundary without mounting a public flow. Short-lived PKCE verifiers use an S3 object vault under a fixed prefix and opaque `vault://ai-media-studio/oauth-pkce/v1/<uuid>` reference. The vault pins the official AWS S3 endpoint, requires a fully qualified customer KMS key ARN in the configured region, writes with SSE-KMS, Bucket Keys, JSON content type and `IfNoneMatch: "*"`, validates encryption/KMS/metadata/envelope bindings before returning a verifier, bounds response bodies, and deletes only exact validated objects. Provider authorization URLs are built from immutable manifests with fixed endpoints, audited scopes, strict HTTPS redirect validation and no client secrets. Current policies omit PKCE for TikTok Web, Meta/Instagram and Google confidential Web Server flows; Google retains offline-consent parameters. Runtime composition is inert: an empty `AI_MEDIA_STUDIO_OAUTH_*` namespace leaves OAuth unavailable, while partial or unknown config fails closed without echoing values.

PR12 adds a durable callback saga with leases, fencing, deterministic code/token bindings, no automatic re-exchange after ambiguous provider I/O, and one short transaction for provider-account credential CAS plus session completion. PR13 supplies the previously abstract authorization-code and long-lived token vaults. It encrypts each secret payload with a fresh KMS data key and AES-256-GCM authenticated by the full canonical context, stores only ciphertext under deterministic immutable S3 keys, and also requires S3 SSE-KMS, the exact CMK account/bucket owner, official AWS endpoints, strict envelopes and generic errors. Token descriptors and bundles are authenticated together; only a separate reader capability returns the bundle. PR14 adds durable cleanup obligations before external writes, PostgreSQL-clock eligibility, bounded `SKIP LOCKED` claims, live fencing, exact source-context revalidation, two delete passes separated by quiescence, and an explicit `runOnce` worker. Its inert infrastructure preflight verifies three distinct unversioned S3/CMK planes twice before producing a short-lived identifier-free attestation.

PR15 separates provider connection into durable exchange, discovery, explicit selection and activation stages. A discovered TikTok user, YouTube channel, Facebook Page or Instagram professional account is stored as bounded tenant/actor/session evidence; even a singleton requires an exact actor confirmation. Selection is an immutable row tied by a relation-exact foreign key to the discovered candidate, and stale workers are fenced at every actionable stage. Required scopes may be a subset of returned scopes only inside the frozen local grant allowlist. Provider task strings are validated and mapped to capabilities locally. Token metadata records only role and discriminated lifetime/revalidation fields; role-specific vault references and cleanup are intentionally deferred to a reviewed v2 contract. The new path is exported but not wired into the older callback saga, routes, timers or provider adapters. Dedicated IAM roles, IaC/Access Analyzer and operational evidence, activation/account CAS integration, vault cleanup v2, sandbox connectors and refresh/revoke proof remain hard gates.

PR16 is a local activation foundation rather than a runtime connector. It binds every activation to the immutable PR15 selection, exact discovered target, credential-version candidate and required artifact-role set. PR16A adds unapplied SQL for credential artifacts, exact account bindings, v2 cleanup obligations, lifecycle guards and deferred graph integrity, with isolated PostgreSQL 16 proof and clean checker/App QA gates. Local PR16B adds PostgreSQL-owned canonical selection evidence, a secret-free staged graph committed before vault I/O, exact activation/replay CAS, expired-staging abandonment, separate role-token-v2 cleanup, and a dedicated AWS v2 preflight. Its focused suite passes 14/14, isolated PostgreSQL evidence passes 3/3, the full suite passes 688/0/34 of 722, TypeScript/build pass, and checker plus App QA are P0=P1=P2=0. No route, runtime worker loop, real AWS/provider operation or deploy is introduced, and the legacy publishing-readiness projection remains intentionally unusable until exact-target binding is integrated in a later reviewed slice.

Analytics accepts normalized snapshots for views, impressions, likes, comments, shares, clicks, watch time, CTR, and retention. Tenant-scoped repositories calculate summaries, direct/last-touch attribution dimensions, and USD cost-per-video/view from durable joins. The fake ingestion adapter proves the port shape only; platform collectors, billing reconciliation, and real attribution validation are missing.

Source intake accepts bounded provider-neutral snapshots, computes canonical content hashes, and deduplicates within tenant/workspace. Fake adapters cover the contract. Live Kong/platform feeds, OAuth, polling/webhooks, and production ingestion are not implemented.

The source orchestrator applies idempotent state transitions with rights, moderation, immutable approval, budget reservation, and kill-switch evidence. The Drizzle repository uses state/version compare-and-swap and writes emissions to the transactional outbox. This proves guarded state-machine and repository behavior in code; no autonomous end-to-end consumer is running.

### Governance and quality gates

PR5 introduces tenant-scoped, append-only influencer governance profiles and asset quality reviews. A new profile or review extends an immutable revision chain; an idempotency key may replay only the same input digest. Durable appends validate tenant ownership, serialize competing revisions with a PostgreSQL advisory transaction lock, and never update or delete prior evidence. Public records omit internal bindings, proof digests, actors, tenant scope, and idempotency fields.

Render policy checks consent/rights basis, validity window, revocation, exact influencer/avatar/voice bindings, allowed use, territory, and required/prohibited brand terms. Generation checks before persistence, retry checks again, and both inline and durable execution revalidate immediately before provider submission so queued work cannot rely on stale authorization.

Quality reviews score natural movement, eye contact, speech quality, lighting, realism, brand consistency, and vertical quality. The service derives `approved`, `needs_review`, or `rejected`; durable evidence is bound to the exact tenant-owned asset checksum. Publishing preview, draft creation, approval, operator retry, and the worker's final provider submission all fail closed unless current influencer governance and an approved current-checksum review pass the server-side gate. Every `PublishingWorker` composition must receive the runtime's mandatory `publishingSubmissionGate`; there is no permissive default. Client governance, review, render-readiness, and publishing guidance are operator surfaces only; they do not replace server enforcement.

This slice does not contain a legal-document or legal-proof vault and does not establish that a proof digest corresponds to a reviewed contract. It also does not perform automated video analysis: quality scores are human-entered evidence, and PR5 persistence accepts only the `human` evaluator type.

### Provider webhook

`POST /api/ai-media-studio/webhooks/providers/:providerKey/accounts/:endpointKey` is exempt from application-user authentication so an external provider can reach it, but the opaque account endpoint must resolve server-side to exactly one tenant/provider account. Production has no provider-only fallback URL and no process-global webhook secret path; development/test may inject a provider-only compatibility harness only for local tests.

HeyGen verification uses HMAC-SHA256 over the exact raw request body and checks active plus unexpired previous secret candidates resolved from server-side secret references. Replay identity and occurrence time are accepted only from signed JSON body fields; if the body has no event id, the route uses a stable raw-body SHA-256 digest as the fallback id. Unsigned request headers such as timestamp or event id are deliberately ignored for HeyGen routing, dedupe, and ordering.

Render job lookup, webhook dedupe, parked callbacks, and worker submission projection are scoped by `(providerAccountId, providerKey, providerJobId/eventId)`. Provider-controlled error text is sanitized before it reaches stored job state or public responses.

Webhook processing is idempotent and fast. The system must not trust remote artifact URLs without allowlisting, redirect limits, size limits, and SSRF protection; reconciliation covers missing callbacks.

## Lifecycle

```text
pending -> rendering -> completed
   |          |
   +----------+-> failed -> retry -> pending
   +----------+-> cancelled
```

Adapters may keep finer internal stages such as submitting, downloading, or retry-scheduled. They map those stages to the shared status while preserving `stage` for display and diagnostics.

## Persistence and automation roadmap

The schema and repositories cover the PR2 core, PR3 source intake/orchestration/publishing/analytics/cost/outbox fields, the PR4 asset-ingest queue and canonical render-output link, PR5 append-only governance/quality evidence plus render-governance snapshots, PR6 account-scoped provider identity, PR8 publishing-account tenant/platform isolation, OAuth control-plane deltas, and PR19–PR27 admission/worker/terminal evidence. In-memory implementations remain development/test references; production is designed to fail closed without configured persistence. Twenty-one forward/rollback migration pairs are checked in under `migrations/ai-media-studio/`, including independently checked local PR16A and PR16B, but none has been applied and `db:push` has not run. Local adapter composition is not proof of a configured production bucket, social connection, database, or worker.

The sole current sequence authority is the preparation-only `docs/ai-media-studio/staging-rehearsal-runbook.md`. It is NO-GO: PR1 baseline provenance is unproven, PR16A and PR16B are unapplied, and PR16B still needs exact stacked GitHub review plus explicit rehearsal approval. PR13 is schema-neutral; PR14 depends on PR12 database controls, while PR13 external storage readiness remains separately unproven. No checked-in paragraph or individual migration note authorizes applying even the earlier deltas. Backup/isolated restore, exact manifest review, drained writers/workers, forward and post-rollback restart/recovery, live storage checks, checker, App QA and Robert's explicit approvals remain hard gates; `drizzle-kit push` and `db:push` are forbidden shortcuts.

The first source pilot should be a Radio Calendar event because the repository already has calendar and radio boundaries. A source event creates ideas/drafts first; it does not render or publish until budget, quality, rights, and approval policies pass. Automatic publishing remains disabled until dedicated connector and App QA gates exist.

## PR3 integration seams

- `server/ai-media-studio/routes.ts` remains the single HTTP adapter/composition root and now mounts PR3 operations routes; no second router mount is allowed.
- `server/routes.ts` keeps exactly one `registerAiMediaStudioRoutes(app)` call. New core endpoints belong in the Studio router, not the global route file.
- Runtime selection remains fail-closed: Drizzle with `DATABASE_URL`, memory only in development/test, and unavailable outside those modes without a database. The operations runtime imports durable repositories lazily and does not open a pool at module import.
- `client/src/pages/ai-media-studio.tsx` and navigation compose Publishing, Analytics, and Automation surfaces through the shared operations DTOs. UI integration does not enable posting.
- PR3 worker/outbox modules are explicit composition dependencies and never autostart from import.
- PR2 must be applied before PR3 in staging. Both remain unapplied until backup and staging approval, followed by restart/recovery and rollback rehearsal.

## PR4 integration seams

- `server/ai-media-studio/assets/**` owns the ingest queue, bounded-reader, owned-storage, delivery-signer, and worker contracts. Fake adapters are development/test evidence only.
- `server/ai-media-studio/routes.ts` composes ingest hooks that materialize one canonical tenant asset and update the render projection. The factory still starts no worker loop and performs no provider download on import.
- Completed-unlinked reconciliation is explicit and bounded; it repairs catalog/render linkage without downloading or uploading the already committed object again.
- Public library and job responses redact provider artifact URLs and storage internals. The authenticated delivery route creates short-lived URLs on demand and fails closed without a configured signer.
- PR4 must be applied after PR2 and PR3 in staging. All three migrations remain unapplied until backup and staging approval, followed by restart/recovery and rollback rehearsal.

## PR5 integration seams

- `shared/ai-media-studio-governance.ts` is the public request/response contract; internal bindings and evidence remain server-owned.
- `server/ai-media-studio/governance/**` owns append-only repositories, evidence digests, profile/review services, and render/publish gate decisions.
- `server/ai-media-studio/routes.ts` remains the single authenticated composition root. Governance persistence follows the existing fail-closed runtime policy and is never selected from client-supplied evidence.
- Render authorization is revalidated adjacent to provider submission, including retries; a UI readiness result is informational and can become stale.
- A quality decision is valid only for the exact current asset checksum. Publishing approval and the immutable publishing-preview digest remain separate required evidence.
- PR5 must be applied after PR4. PR2, PR3, PR4, and PR5 remain unapplied; staging/live-browser App QA and Robert deployment approval remain separate release gates.

## Scale, security, and cost

Ten thousand videos per day remains an architectural target. PR3 includes a deterministic 10,000-job fake-provider rehearsal to exercise arithmetic assumptions; it is not a load test, benchmark, provider-quota proof, storage-throughput proof, or capacity claim. The production-assets adapters add no capacity evidence. Readiness still requires live PostgreSQL contention, horizontal render and ingest workers, backpressure, real provider and storage quotas, observability, cost controls, failure injection, restart testing, country-specific policy evidence, a live browser pass, and staging/deployment proof.

- Secrets are environment/vault references and are never returned or logged.
- Avatar and voice records require provenance, consent, and usage rights.
- Provider callbacks require HMAC and replay protection.
- Remote downloads require SSRF defenses and content validation.
- Estimated and actual costs attach to every generation; retries share its budget.
- Automatic publishing and paid rendering default off.
