# AI Media Studio

AI Media Studio is Kong's provider-neutral content production domain. It owns the workflow from source intake and script/influencer selection through rendering, reusable assets, approval-gated publishing records, and normalized analytics. External publishing and provider ingestion remain behind explicit release gates.

Draft PR [#171](https://github.com/robertmanzanillag-jpg/blackops-reminder/pull/171) adds the first mounted provider-neutral source synchronization boundary. A strict real-session, exact-origin JSON request can select only a server-registered adapter key and a bounded 1–100 snapshot. The service validates and copies the entire snapshot before tenant-scoped deduplicated persistence, returns only redacted Studio-owned status data and leaves downstream script/render/publishing effects false. No production Kong reader or scheduler is installed; rights/moderation, source-to-script orchestration, video generation, spend and deployment remain separately gated.

The current stacked source-to-script slice adds a provider-neutral `KongOwnedSourceAdapter` over an injected server-side `KongSourceReader`, covering all eight requested categories without construction I/O. After sync, a strict exact-origin source eligibility review can approve or reject only the exact current `contentHash`, and a separate script-preview route can read only accepted owned/licensed and moderation-approved sources. The Automation workspace exposes those explicit decisions and a compact title/hook/caption preview. Browser callers send Studio source IDs and bounded script options, not provider IDs, cursors, source snapshots or payloads. The preview is deterministic and blocks every downstream effect: no durable script insert, orchestration run, render, outbox, video-provider/HeyGen call, secret resolution, spend, publishing, migration or deployment. A production implementation of `KongSourceReader` and its scheduler remain unconfigured.

## Ownership

The AI Media Studio Lead owns the contracts, architecture decisions, roadmap, and integration gates in this directory. Backend, frontend, automation/data, and QA agents work in non-overlapping file areas and hand changes back to the Lead for integration.

## Current delivery stack

- PR #67 (`codex/ai-media-studio`) is the provider-neutral vertical foundation.
- PR2 (`codex/ai-media-studio-core`) is stacked on that foundation and adds the durable core and owned-media model.
- PR3 lives on `codex/ai-media-studio-operations`, stacked on PR2. Its GitHub review state is tracked outside this document.

The current PR3 code slice adds provider-neutral contracts, repositories, services, fake adapters, HTTP routes, and UI surfaces for manual/scheduled publishing drafts, server-generated immutable previews and approval, scheduling/reconciliation, normalized analytics and attribution/cost summaries, deduplicated source intake, guarded orchestration with compare-and-swap/outbox emissions, and reusable worker/outbox operations. The single Studio router now composes these APIs and remains fail-closed outside development/test when durable persistence is unavailable.

This is code and test evidence, not operational evidence. Focused AI Media Studio tests, TypeScript, independent checker review, security review, and static App QA cover the implemented slices. No live browser target was available. No real publishing OAuth or platform adapter is connected, no external post is authorized, and no live provider ingestion has run. The reviewed PR2 through PR6 plus PR8 migrations are checked in but unapplied; `db:push` has not run and must not be used as a substitute for reviewed migration execution. Live PostgreSQL, staging restart/recovery, real load, and live-environment QA remain pending.

The migration directory now contains a reviewed-local PR1 foundation pair plus
all later reviewed deltas: 22 forward/rollback pairs in one exact chain.
`migrations/ai-media-studio/manifest.json` binds the order and SHA-256 digests
to PR1 foundation commit `8b30f184`, source blob
`4678f3b60595fe272ce11999806a4634317edb03` and source SHA-256
`560ac47625eb1a14297a5a5d127be7cc267d5de3c1943d51ea7e19640be1972d`. This closes the
missing-local-artifact gap; it does not prove a staging catalog or authorize a
connection, migration, `db:push`, provider call, spend, publication, or deploy.
The PR1 rollback is empty-baseline-only and cannot erase evidence retained by
later data-preserving rollbacks.

The deterministic 10,000-job rehearsal exercises arithmetic and fake-provider assumptions only. It is explicitly not evidence of 10,000-video/day capacity.

The current production-assets slice adds a streaming Node HTTPS artifact reader, S3/R2-compatible multipart owned storage, short-lived S3-compatible delivery signing, and a strict `AI_MEDIA_STUDIO_ASSET_*` composition factory. Configuration is atomic and fail-closed: an empty namespace leaves delivery unavailable, while any partial or unsafe namespace rejects startup with a generic error. An explicitly injected signer takes precedence for controlled composition tests. The worker factory starts no timer, process, or network request, and repository presence is never treated as a worker heartbeat.

This remains local code/test evidence. No live artifact was downloaded, no object was uploaded or signed against a real account, no worker heartbeat exists, no migration was applied, and no deploy was performed. Production worker operation, lifecycle/retention configuration, staging restart/recovery, throughput, and provider/storage quota evidence remain release gates.

The publishing-account slice reuses the server-owned provider-account identity as a vault-reference-only social connection record. Durable readiness reads are tenant/workspace scoped, expose one internal connection ID at most, allow only four provider-neutral capabilities, and fail closed when accounts are incomplete or ambiguous. Publishing-job account references are constrained to the same tenant, workspace, and platform. No endpoint accepts a token or secret value, OAuth is not implemented, and automatic publishing remains disabled.

The PR9 control-plane slice adds provider-neutral, durable OAuth session contracts without enabling a provider flow. It generates 48-byte opaque state, persists only its SHA-256 digest, requires PKCE S256, stores the verifier only through a purpose/version-scoped vault reference, and atomically consumes an unexpired state once using only the digest plus platform fence. Sessions retain the exact tenant, actor, account, redirect and requested-scope snapshot behind a composite tenant/workspace/account/platform foreign key. Provider credential metadata defaults all legacy accounts to `unverified` and version zero; a social connection is unusable until a future approved connector records an active, versioned, unexpired credential.

The PR10 managed-OAuth-vault slice adds internal-only production composition for short-lived PKCE verifier storage and provider authorization URLs. The S3 PKCE vault stores only bounded JSON verifier envelopes under `vault://ai-media-studio/oauth-pkce/v1/<uuid>` references, writes exact keys with SSE-KMS, a customer KMS key ARN, S3 Bucket Keys and `IfNoneMatch: "*"`, validates encryption/metadata/envelope bindings on read, and deletes only exact validated objects. The authorization URL builder uses fixed audited endpoints/scopes from the platform manifests: TikTok Web has no PKCE parameter, while Meta/Instagram and the current Google confidential Web Server policy omit PKCE. Google still applies offline-consent parameters. The production runtime is all-or-nothing for `AI_MEDIA_STUDIO_OAUTH_*`; absent config leaves OAuth unavailable, while partial, unknown or unsafe config fails closed with a generic error. Construction performs no AWS, database or provider I/O.

PR12 adds the durable fenced callback saga and atomic credential-version/account binding without mounting it. PR13 adds inert authorization-code and long-lived token adapters with application-level KMS envelope encryption (AES-256-GCM) plus S3 SSE-KMS, immutable conditional writes, exact contextual AAD/KMS digests, deterministic recovery, strict absence/error handling and a separate token-secret reader capability. PR14 adds a dedicated durable cleanup outbox with fenced leases, exact source revalidation, two-pass deletion, conservative active-token retention, and an inert double-snapshot S3/KMS posture preflight. PR15 adds a separate provider-neutral staged-connection contract for exchange, discovery, explicit target selection and later activation, plus durable candidate and immutable selection evidence. It never selects the first provider result, derives capabilities locally from verified tasks, and persists only safe token-role/lifetime descriptors rather than tokens or vault references.

PR16 is preserved as draft PR #97 on `codex/ai-media-studio-provider-activation-cas`, with PR16A in draft PR #121 and reviewed PR16B in draft PR #124. Its bounded scope adds an exact selected-target/account activation CAS, a role- and target-bound OAuth vault schema v2 with one secret per artifact role, and additive activation/binding/vault-operation declarations. PR16A supplies the unapplied lifecycle/deferred graph integrity pair and isolated PostgreSQL 16 proof. PR16B adds PostgreSQL-owned canonical selection evidence, a prewrite durable binding/artifact/cleanup graph, exact staged-finalization CAS, indeterminate crash recovery, fenced two-pass role-token-v2 cleanup, and a dedicated stable-double-snapshot AWS v2 preflight. Focused evidence is 14/14, the isolated PostgreSQL suite is 3/3, the complete suite is 688/0/34 of 722, TypeScript/build pass, and checker plus App QA are P0=P1=P2=0. This remains NO-GO and unapplied pending explicit rehearsal approvals and staging evidence. Every PR16B component remains unexported from production composition and unmounted: no route, timer, real vault/provider call, post, spend or deployment is enabled. Effective IAM/IaC evidence, activation orchestration, sandbox connectors, refresh/revocation and provider proof remain mandatory before any callback or publishing route is enabled.

PR17 is preserved as draft PR #99 on `codex/ai-media-studio-heygen-launch-roster`, stacked on PR #97. It adds a launch-only roster wizard for 5–10 HeyGen avatars, with 10 videos planned per avatar (50–100 total), durable tenant-scoped mappings, and draft provider-neutral influencer/resource records. The onboarding request is the one narrow boundary allowed to carry `avatar_id` and `voice_id`; responses, URLs, browser storage, logs, and public catalog DTOs omit those native identifiers. The inert render adapter now follows HeyGen Studio V2 (`POST /v2/video/generate`) and accepts only `data.video_id`. Roster setup does not enqueue or generate a video, spend credits, prove consent/rights, or authorize deployment. A unique active/verified server-side HeyGen account is still required; account/API-key provisioning, governance approval, cost admission, sandbox proof, and a separate launch approval remain blockers.

PR18 is preserved as draft PR #100 on `codex/ai-media-studio-roster-daily-plan`, stacked on PR #99. Reviewed draft PR #136 on `codex/ai-media-studio-durable-roster-plan-bridge`, stacked on draft PR #131, replaces its derived preview with one atomic roster transaction: 5–10 avatars create one blocked durable daily plan and exactly 10 blocked durable slots each (50–100 total) in the existing PR19 tables. PostgreSQL supplies the timestamp and the trusted server-owned timezone supplies the accounting date; exact replay returns the same roster and plan without duplicating rows. The public route keeps the PR18 response shape with `not_queued` slots, `canGenerate=false`, `noSpendGuarantee=true`, and no provider-native or private database identifiers. It creates no script, budget reservation, render job, outbox command or provider submission, and it does not call HeyGen, publish, apply a migration or deploy.

Reviewed draft PR #141 on `codex/ai-media-studio-durable-script-batch`, stacked on PR #136, prepares deterministic scripts for all 50–100 blocked plan slots without creating launch or provider work. The current `codex/ai-media-studio-launch-readiness` slice adds the full selected creative review—including Video Title—and one atomic whole-batch approval. A canonical creative digest seals title, angle, hook, script, CTA, caption, hashtags and SEO keywords. Scripts, their selected/current variants, slots and plan move together only after exact tenant, batch, source eligibility, content checksum and full-creative-digest validation; unselected alternatives remain draft and unapproved. The approved response still fixes `canGenerate=false` and `noSpend=true`. Approval does not issue governance/content authority, a launch intent/snapshot, sandbox/quote proof or human launch authority; it does not reserve budget, create render/outbox work, call HeyGen, spend, publish, apply any of the 22 migration pairs or deploy.

Current launch-readiness evidence is reviewed-local: focused checks pass 20/20, authenticated HTTP checks pass 2/2, the owned PostgreSQL 16 exact-chain harness passes 1/1, and TypeScript/production build pass. Its database cases reject approval after rights/moderation/status revocation, detect CTA/Video Title/current-variant-pointer tampering, and prove 400 unselected alternatives remain draft and approval-free. The final full run records 722 passed, one inherited PR26 message-regex mismatch and 40 controlled PostgreSQL-only skips out of 763; the prior HeyGen client regression is fixed and its owned set passes 13/13. Independent checker, security review and App QA are clean at P0=P1=P2=P3=0. The slice is ready for a draft GitHub checkpoint, not merge, migration application, provider use, spend or deployment.

The current `codex/ai-media-studio-offline-launch-preflight` slice is stacked exactly on draft PR #144 and adds an authenticated `GET /production-batches/:planId/launch-preflight`. It is a coherent, PostgreSQL-clock-owned, derived read-only observation of the approved PR #144 batch and 14 ordered gates: batch integrity, plan window, source eligibility, local provider binding, governance coverage, launch intent, content approval, policy/kill switch, live provider verification, maximum quote, sandbox proof, human launch approval, authority snapshot, and budget/admission capacity. The UI requires a separate confirmation for each of the 5–10 creators before whole-batch script approval, then exposes the 14-gate observation only for the exact approved batch identity. It reports 10 scripts per creator (50–100 required slots), safe reason/next-action codes and `canGenerate=false`, `sandboxExecutionAllowed=false`, `spendAuthorized=false`, `noSpend=true`, and `authoritativeForAdmission=false`.

This preflight creates no launch intent, evidence, authority snapshot, budget reservation, render or outbox row and makes no provider call. It cannot migrate, deploy, publish or spend; external setup, human decisions and later admission remain separate controlled workflows. Review corrections make source-title completeness part of source eligibility, reject a future `valid_from` policy/kill-switch window, align capacity observation with the admission boundary, and preserve approval-success/focus sequencing within the approval transition. Creator confirmation changes only the local confirmed-member set; the approval mutation resets only when the external batch identity changes. Final local evidence passes 54/54 focused checks, 2/2 authenticated route checks, 1/1 owned PostgreSQL 16 exact-chain check, TypeScript and production build. The final full AI Media Studio run contains 791 tests: 749 pass, 1 inherited PR26 wording-regex failure and 41 skip; this slice does not change that baseline test. No PR URL has been assigned to this branch yet.

Draft PR [#147](https://github.com/robertmanzanillag-jpg/blackops-reminder/pull/147) on `codex/ai-media-studio-one-video-sandbox-readiness` is stacked on draft PR #146. It adds an authenticated, read-only one-slot sandbox packet for one exact approved plan, batch and slot. The packet exposes a `9:16` vertical preview, six local readiness gates and five explicit external requirements while keeping generation, provider submission, admission, spend and every effect flag false. The workbench requires an explicit approved-slot selection and provides a browser-verified click path without adding an Execute, Generate or Spend action. Malformed 5–10 × 10 preflight data now fails closed with a structured repair outcome instead of appearing launchable.

The owned PostgreSQL 16 rehearsal applies the exact 22-pair migration chain to a disposable local cluster, seeds a genuine 5 × 10 batch with all 50 scripts approved, and drives exactly one slot through real admission, activation, terminal observation and durable ingest using an injected fake provider. It proves one fake submission, one canonical owned asset/render link and zero publishing records; it does not prove HeyGen connectivity, provider billing, external storage, production deployment or live capacity. Supporting fixes preserve PostgreSQL 16 compatibility for casts, advisory-lock JSON input and the full PR27 projection. Final local evidence is focused 59/59, authenticated routes 4/4, browser 1/1, PostgreSQL 1/1, TypeScript, production build and diff hygiene. The complete suite ran 812 cases: 811 passed and only the inherited PR26 wording-regex case failed; checker, security and App QA are clean at P0=P1=P2=P3=0 after a P3 test correction.

No real API key, secret, network/provider request, credit spend, migration application, deployment or publication is part of draft PR #147. Robert can provide the HeyGen API key through the approved secret manager plus the 5–10 avatar and voice IDs. Live provider verification, a maximum quote, owned-storage and callback readiness, and explicit one-video cost approval must pass before the first real video. The initial 5 × 10 canary remains a later, separately cost- and deployment-approved step.

Draft PR [#148](https://github.com/robertmanzanillag-jpg/blackops-reminder/pull/148) on `codex/ai-media-studio-heygen-onboarding-readiness`, stacked on draft PR #147, adds the missing safe onboarding control plane. It stores only an approved `env://AI_MEDIA_STUDIO_SECRET_HEYGEN_API_KEY[_SUFFIX]` reference and digest with exact tenant/account/version rotation; the key value is never accepted by HTTP, returned, logged or stored in PostgreSQL. The authenticated read-only panel guides the initial 5–10 avatars and ten videos each. Avatar/voice IDs may be recorded as `pending_verification`, but their public state is `inactive`, they cannot be selected for an influencer, and all 50–100 daily slots remain blocked with no budget, render, outbox, provider, publishing or spend effect.

The PR28 SQL remains in `migrations/ai-media-studio/pending/`, outside the manifest and unapplied. A disposable PostgreSQL 16 rehearsal of the exact prior chain plus forward binding, rotation and guarded rollback passes 1/1. Final App QA targeted evidence passes 44/44; TypeScript, production build, codebase map and diff hygiene pass; independent checker, security and App QA are P0=P1=P2=P3=0. Next, Robert places the API key only in the deployment secret manager and provides 5–10 avatar/voice ID pairs. Read-only live verification, a maximum quote and explicit one-video cost approval remain separate gates; no HeyGen call or credit spend has occurred.

Draft PR [#149](https://github.com/robertmanzanillag-jpg/blackops-reminder/pull/149) on `codex/ai-media-studio-one-video-execution-control`, stacked on draft PR #148, adds an authenticated read-only control for one exact approved slot. It exposes only public creator/avatar/voice labels, `9:16 MP4`, persisted verification/maximum-quote/human-approval states and explicit blockers. The native Execute button is permanently disabled and has no handler; the server installs only GET, uses a repeatable-read/read-only PostgreSQL observation, redacts native/provider/secret/digest identities and fixes every provider, approval, budget, render, publishing and spend effect to false. The browser path is exact-slot and GET-only, including rapid slot changes. This slice intentionally cannot report a current verified static credential until a later immutable live-verification evidence model exists, and it remains blocked by `one_shot_executor_not_installed` even if all future evidence becomes current.

The pinned provider contract for this separately approved work is [HeyGen V3 provider profile](./heygen-v3-provider-profile.md). Draft PR [#154](https://github.com/robertmanzanillag-jpg/blackops-reminder/pull/154) on `codex/ai-media-studio-heygen-verification-evidence`, stacked on draft PR #149, prepares an inert, unmounted V3 verifier that can later perform only exact `GET` reads for the current account, 5–10 avatar looks, their parent groups and the deduplicated voices. Responses are time- and size-bounded, native IDs remain server-only, and no API-key-derived value enters evidence. A serializable repository binds a passed result to the exact static credential version, pending roster resources and blocked 5–10 × 10 plan, then records append-only header/resource evidence and exact activation pointers. PostgreSQL 16 proves clean forward/rollback, tenant isolation, exact avatar-look/group-consent/voice coverage, rotation invalidation and UPDATE/DELETE/TRUNCATE protection. The pending PR29 migration remains outside the manifest and unapplied.

Robert must still place the API key in the deployment secret manager rather than chat or GitHub, then provide the exact 5–10 HeyGen avatar **look** IDs and intended voice IDs. Resolving that secret and making the first read-only live verification require a separate approval. Maximum quote, Robert-bound one-video spend approval, a dedicated run-once executor with concurrency one, migration application and Replit deployment are later independent gates. No live verification, quote request, approval write, provider request, migration application, spend, deployment or publication is part of this slice.

Draft PR [#156](https://github.com/robertmanzanillag-jpg/blackops-reminder/pull/156) on `codex/ai-media-studio-one-video-verification-read-model`, stacked on draft PR #154, closes the read-side gap between that immutable PR29 evidence and the one-video control. The authenticated GET can display `verified` only when the exact tenant, account, credential version, plan/roster, avatar-look and voice evidence graph is current; expiry, rotation, incomplete evidence, cross-tenant identity or later native-ID drift fails closed. Disposable PostgreSQL 16 proves both the initial 5 × 10 and maximum 10 × 10 shapes. Execute remains disabled, all effects remain false, and this checkpoint performs no HeyGen request, secret resolution, quote, approval write, spend, publishing, migration application or deployment.

PR19 is preserved as draft PR #102 on `codex/ai-media-studio-durable-daily-admission`, stacked on PR #100. It adds provider-neutral pure admission contracts, four durable PostgreSQL table declarations, reviewed-but-unapplied forward/rollback SQL, and an unexported/unmounted reservation-only Drizzle transaction for daily plans, slots, micro-USD budget buckets, and immutable reservations. The transaction uses PostgreSQL wall-clock time, tenant/workspace and governance advisory locks, exact account/credential and row locks, idempotent replay, and one budget/reservation/slot CAS. Approval, sandbox, policy, quote, kill-switch, and governance evidence is digest-bound, but the non-governance evidence is not yet backed by a durable authority; therefore this repository is not runtime-ready. Reserved money moves to committed immediately before a future provider submission and cannot auto-refund after an ambiguous response. This slice mounts no route, creates no render job/outbox command, calls no provider, applies no migration, spends nothing, and deploys nothing.

PR20 is preserved as draft PR #103 on `codex/ai-media-studio-durable-authorities`, stacked on PR #102. It replaces admission caller self-certification with append-only policy, kill-switch, launch-evidence, and exact launch-authority snapshot records. The reservation request now carries only tenant/plan/slot/bucket identity, an authority snapshot ID and digest, expected state versions, expiry, and idempotency evidence; provider identity, governance, approvals, sandbox result, quote amount, policy, kill switch, and provider idempotency are derived from locked database rows. A global admission advisory lock makes total/provider/tenant reservation counts race-safe in this inert scaffold; production-scale lease counters remain a later optimization. The migration and repository remain unapplied, unexported, and unmounted. Authenticated authority issuance and a live PostgreSQL contention/migration rehearsal are still required before runtime activation. This slice creates no job or outbox command, calls no provider, spends nothing, and deploys nothing.

PR21 is preserved as draft PR #104 on `codex/ai-media-studio-authority-issuers`, stacked on PR #103. It adds an unmounted server-only authority service with seven separate capabilities, narrow human decisions, workload-only sandbox/quote attestations, append-only Drizzle issuers, exact idempotent replay, PostgreSQL-owned identity/time, shared workspace/governance lock ordering, and immutable snapshot construction from the newest complete evidence chains. A PostgreSQL 16.14 socket-only ephemeral harness applies the exact PR19 and PR20 forward SQL over an explicit test-only prerequisite fixture and proves syntax, constraints, in-place immutability, tenant isolation, concurrent revision/idempotency conflicts and non-destructive rollback behavior. This is not a staging-copy migration, restart, throughput, provider, or spend proof. The issuers remain absent from HTTP and public barrels because trusted production RBAC/authenticators, runtime-minted (not TypeScript-brand-only) sandbox/quote attestations, durable launch-subject intent, budget-bucket provisioning, scale counters, sandbox generation and Robert's spend/deploy approval are still required.

PR22 is preserved as draft PR #105 on `codex/ai-media-studio-launch-intent-attestations`, stacked on PR #104. It adds an immutable launch intent for each exact tenant/slot/attempt and binds plan, roster member, provider credential version, approved script/source checksum, current governance revision, use, territory and country before any evidence can be issued. Non-manual sources must retain the exact canonical hash, an accepted/ready state, approved moderation and owned/licensed rights; their row is locked through final admission, while a later content refresh invalidates stale intent instead of being blocked by a hash foreign key. Sandbox and quote commands now carry only opaque, unguessable handles; a server verifier resolves their trusted result after the exact durable subject is locked and PostgreSQL time is read, and the external attestation identity/digest is retained for audit reconstruction. Evidence, snapshots and both admission guards require the same intent identity. The included process-local issuer/verifier is reference and test composition only, not a production distributed attestation service. The migration remains reviewed-but-unapplied and the repository remains unmounted: no route, provider call, render job, outbox command, credit spend, post or deployment is enabled. Production RBAC, a durable distributed verifier, bucket provisioning, staging rehearsal, an approved small HeyGen sandbox and Robert's explicit spend/deploy approval remain gates.

## Documents

- [Architecture](./architecture.md)
- [ADR 0001: Provider-neutral modular boundary](./adr-0001-provider-neutral-boundary.md)
- [Delivery board](./kanban.md)
- [Requirements matrix](./requirements-matrix.md)
- [GitHub recovery checkpoint — 2026-07-21](./github-checkpoint-2026-07-21.md)
- [OAuth provider readiness and sandbox gate](./oauth-provider-readiness.md)
- [Source snapshot and script variants](./script-generation-contract.md)
- [Durable persistence readiness](./persistence.md)

## API contract

Foundation DTOs live in `shared/ai-media-studio.ts`; PR3 publishing, analytics, source, and policy DTOs live in `shared/ai-media-studio-operations.ts`; PR9/PR10 transient OAuth control-plane DTOs live in `shared/ai-media-studio-oauth.ts`. Frontend and backend must import those definitions rather than creating provider-specific response types.

The authenticated API root remains `/api/ai-media-studio`. The production-batch surface uses `GET /production-batches/current`, `POST /production-batches/:planId/prepare-scripts`, `POST /production-batches/:planId/approve-scripts`, the derived read-only `GET /production-batches/:planId/launch-preflight`, and the read-only `GET /production-batches/:planId/sandbox-readiness/:slotId` for one exact approved slot; every production-batch, preflight and sandbox-readiness response remains unable to generate or authorize spend, and the legacy direct generation/retry bypasses fail closed behind plan admission. Provider callbacks use `/api/ai-media-studio/webhooks/providers/:providerKey/accounts/:endpointKey`; the route is public only for reachability, resolves the opaque endpoint to exactly one tenant/provider account, and requires verified raw-body provider HMAC. Production does not use the legacy provider-only callback or process-global webhook secrets. PR3 adds mounted routes for publishing previews/jobs/connections, analytics summaries/attribution, sources, and the read-only automation policy. Mounted code is not deployment evidence: no real connector, migration, or external post is enabled.

## Release guardrails

- Work is PR-first; never commit an agent-generated fix directly to `main`.
- A second agent must review the diff and run relevant checks.
- App QA route, click/link, API, error, and improvement scouts are release gates.
- Any QA warning blocks deployment.
- Replit deployment requires Robert's explicit approval after the PR/QA summary.
- Paid rendering, automatic publishing, external posting, or increased spend requires Robert's approval and a cost estimate.
- Manual or scheduled publishing records never authorize a platform post without matching immutable-preview approval and a configured, approved connector.
